import React from "react";
import { shallow } from "enzyme";

import { SkympEmailIcon } from "./index";

describe("SkympEmailIcon", () => {
  it("renders without errors", () => {
    shallow(<SkympEmailIcon />);
  });
});
